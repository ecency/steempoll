'use strict';

describe('steempoll.poll module', function() {

  beforeEach(module('steempoll.poll'));

  describe('poll controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pollCtrl = $controller('PollCtrl');
      expect(pollCtrl).toBeDefined();
    }));

  });
});
