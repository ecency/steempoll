'use strict';

describe('steempoll.new module', function() {

  beforeEach(module('steempolls.new'));

  describe('new controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var newCtrl = $controller('NewCtrl');
      expect(newCtrl).toBeDefined();
    }));

  });
});
